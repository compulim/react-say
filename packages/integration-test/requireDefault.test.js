const { scenario } =require( '@testduet/given-when-then');
const { expect } =require( 'expect');
const { afterEach, beforeEach, describe, it } =require( 'node:test');
const { Say } =require( 'react-say');

scenario(
  '<Say> component',
  bdd =>{},
  {
    afterEach,
    beforeEach,
    describe,
    it
  }
);
